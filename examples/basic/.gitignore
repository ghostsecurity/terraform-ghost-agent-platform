# Local state — never commit
.terraform/
.terraform.lock.hcl
terraform.tfstate
terraform.tfstate.backup
terraform.tfstate.*.backup
*.tfplan
crash.log

# Real values — never commit
terraform.tfvars
*.auto.tfvars
